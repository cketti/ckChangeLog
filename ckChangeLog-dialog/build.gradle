apply plugin: 'com.android.library'

android {
    compileSdkVersion propCompileSdkVersion
    buildToolsVersion propBuildToolsVersion

    defaultConfig {
        minSdkVersion 4

        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    compile project(':ckChangeLog-core')
}
